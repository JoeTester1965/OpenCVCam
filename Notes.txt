Add in motion detect mask - save image with edges to draw - is a whitelist and blacklist.

Add min box side

Add in motion vector -: at least x (2?) overlapping (with some expansion factor) boxes within y (2*fps?) frames 

Camera up / down management

Add in simple person detect model.
